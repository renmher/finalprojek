version: "3.8"
services:
  backend:
    image: haechanlovelove/todo-backend:latest
    ports:
      - "5000:5000"
    restart: always

  frontend:
    image: haechanlovelove/todo-frontend:latest
    ports:
      - "80:80"
    depends_on:
      - backend
    restart: always

